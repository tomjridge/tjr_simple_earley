FROM impfs_base_docker_image

RUN echo edit to rebuild....

RUN eval `opam config env` && opam install dune

RUN eval `opam config env` && opam pin add -y -n tjr_lib_core https://github.com/tomjridge/tjr_lib.git 
RUN eval `opam config env` && opam install -y tjr_lib_core
RUN eval `opam config env` && opam pin add -y -n tjr_lib https://github.com/tomjridge/tjr_lib.git 
RUN eval `opam config env` && opam install -y tjr_lib
