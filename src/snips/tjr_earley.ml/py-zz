
*)
open Bitms_lt_k_ops

end
  }
    ltk_remove:'k -> 't -> 't;
    ltk_empty:int -> 't;  (* need to*)
    ltk_find:'k -> 't -> 'v;
    ltk_add: 'k -> 'v -> 't -> 't;
  type ('k,'v,'t) ltk_map_ops = {
module Bitms_lt_k_ops = struct
(*
